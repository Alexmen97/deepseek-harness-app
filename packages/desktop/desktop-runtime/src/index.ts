/**
 * Desktop runtime app package: the identity provider, the process runner the
 * bins share, and the runtime composition shipped beside them.
 *
 * @module @deepseek-ai/dsh-desktop-runtime
 */

export { runDesktopRuntime, NAME } from './runner.ts'
